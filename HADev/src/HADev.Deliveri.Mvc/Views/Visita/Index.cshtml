@model IEnumerable<HADev.Delivery.Domain.Models.Visita>

@{
    ViewData["Title"] = "Visita";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista de Visitas</h2>

<p>
    <a asp-controller="Visita" asp-action="Create" class="btn btn-info"><span class="fa fa-plus-circle"></span>&nbsp;Adicionar</a>
</p>
<table class="table table-bordered table-hover dataTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Eleitor.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataVisita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Obs)
            </th>

            <th>Ação</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Eleitor.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataVisita)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Obs)
                </td>

                <td>
                    <a asp-controller="Visita" asp-action="Edit" asp-route-id="@item.VisitaId"><span class="fa fa-pencil"></span></a> &nbsp;&nbsp;
                    <a asp-controller="Visita" asp-action="Details" asp-route-id="@item.VisitaId"><span class="fa fa-search"></span></a>&nbsp;&nbsp;
                    <a asp-controller="Visita" asp-action="Delete" asp-route-id="@item.VisitaId"><span class="fa fa-trash"></span></a>
                </td>
            </tr>
        }
    </tbody>
</table>
